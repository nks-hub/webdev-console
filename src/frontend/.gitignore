node_modules/
out/
dist-electron/
release/
daemon/bin/
daemon/obj/
*.user

# Playwright runtime artifacts — tests/playwright/results/junit.xml is
# the configured reporter output (defined in playwright.config.ts) and
# test-results/ accumulates per-failure traces + screenshots. Both are
# regenerated by every `npx playwright test` invocation.
test-results/
tests/playwright/results/
playwright-report/
